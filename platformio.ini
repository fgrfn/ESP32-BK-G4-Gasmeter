; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200

; Board-spezifische Einstellungen für ESP32-WROOM-32D
board_build.mcu = esp32
board_build.f_cpu = 240000000L
board_build.f_flash = 40000000L
board_build.flash_mode = dio
board_build.partitions = default.csv

; Build-Flags
; Uncomment to enable debug output:
; build_flags = 
;     -DDEBUG_MODE
;     -DDEBUG_MBUS
;     -DDEBUG_MQTT
;     -DDEBUG_WIFI
;     -DENABLE_ANSI_COLORS

lib_deps =
    knolleary/PubSubClient @ ^2.8
    marian-craciunescu/ESP32Ping @ ^1.7
    bblanchon/ArduinoJson @ ^7.0.4
    https://github.com/me-no-dev/AsyncTCP.git
    https://github.com/me-no-dev/ESPAsyncWebServer.git

; USB-Upload (für initialen Flash oder wenn OTA nicht verfügbar):
upload_speed = 921600
; OTA-Updates (auskommentieren wenn ESP erreichbar):
;upload_protocol = espota
;upload_port = 10.10.40.102

; === ALTERNATIVE ENVIRONMENTS ===

; Debug build mit allen Debug-Ausgaben
;[env:esp32dev-debug]
;platform = espressif32
;board = esp32dev
;framework = arduino
;monitor_speed = 115200
;board_build.mcu = esp32
;board_build.f_cpu = 240000000L
;board_build.f_flash = 40000000L
;board_build.flash_mode = dio
;board_build.partitions = default.csv

;build_flags = 
;    -DDEBUG_MODE
;    -DDEBUG_MBUS
;    -DDEBUG_MQTT
;    -DDEBUG_WIFI
;    -DENABLE_ANSI_COLORS

;lib_deps =
;    knolleary/PubSubClient @ ^2.8
;    marian-craciunescu/ESP32Ping @ ^1.7

;upload_speed = 921600

